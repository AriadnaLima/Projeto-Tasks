import React from "react";

export default props => (
    <nav className="navbar">
        <div className="container-fluid bg-light">
            <div className="navbar-header">
                    <span className="fw-bold">My AppTasks</span>
            </div>
        </div>

    </nav>
) 